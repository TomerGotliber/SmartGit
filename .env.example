# Fine-grained or classic PAT: repo read + read:org (for team member expansion)
GITHUB_TOKEN=

# Comma or whitespace separated: owner/repo
REPOS=my-org/engine,my-org/web-app

# Optional
PORT=4000
POLL_INTERVAL_MS=60000
LOG_LEVEL=info
