# Fine-grained or classic PAT: repo read + read:org (for team member expansion)
GITHUB_TOKEN=

# Comma- or space-separated owner/repo, or * / ALL to scan every non-archived repo this token can access
REPOS=*

# Optional
PORT=4000
POLL_INTERVAL_MS=60000
LOG_LEVEL=info
